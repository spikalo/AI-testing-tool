# Resultaten — NN Layer Test

Opgeslagen runs van `nn-layer-test.html`, één JSON per run
(`resultaten_<taak>_<JJJJMMDD-uummss>.json`). Bekijk en vergelijk ze met
`resultaten.html`.

Runs van de **Ollama LLM-test** staan in `../resultaten-llm/`.
